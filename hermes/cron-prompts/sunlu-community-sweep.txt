You are running a periodic community deal sweep for a product the user price-watches: the Sunlu AMS Filament Dryer/Heater for Bambu Lab AMS Gen 1 (the "Sunlu AMS Heater"). Pricewatch tracker 1 already monitors West3D (about CA$118) and amazon.ca ASIN B0FQVHHBQV (sticker about CA$167.73). The engine now reads Amazon's on-page clip coupon, so `price` from get_price is the after-coupon figure and `extra.deal_note` explains it. Your job: find NEW community intel - above all the recurring Amazon clip coupon that takes this heater to about $49.99 - and tell the user in time to use it.

Steps:
1. Call community_pulse(query="Sunlu AMS heater dryer", days=2, limit=30) and community_pulse(query="Sunlu dryer coupon OR deal", days=2, limit=20).
2. Keep only actionable items: a concrete price or coupon claim about the Sunlu AMS heater/dryer itself, a new retailer listing for it, or a time-boxed Sunlu/West3D/Amazon promo. Ignore general filament chatter, other dryers (Sunlu E2/S2/S4, EIBOS, Sovol), mod and print-file posts, and complaints without pricing.
3. For each candidate call read_reddit_thread on its URL: price, region, coupon, expiry, whether it is still live.
4. Verify with get_price on https://www.amazon.ca/dp/B0FQVHHBQV (and on any listing URL the thread gives). If get_price shows the coupon, the deal is verified. If it does not, say "coupon not visible from here (lapsed or account-specific)" - do not call the claim false.
5. Push with mcp__pricewatch__notify_user (tracker_id=1, url=the listing, price=the claimed figure) whenever a claim about this product is concrete and less than 24 hours old - even when you could not verify it live, and even if a commenter says it just expired: this coupon recurs every few days and the user wants to hear about it early, not after the fact. State verified/unverified and live/expired in the message. Before pushing, call list_alert_events and skip a thread that was already pushed (kind "agent") unless something changed. Never use curl for notifications. The standing West3D and Amazon sticker prices are not news.
6. Final response: a short log - queries run, candidates, what was pushed or why nothing was. One or two lines when quiet.
